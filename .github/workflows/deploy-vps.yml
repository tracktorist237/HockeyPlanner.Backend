name: Deploy Backend to VPS

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy backend on VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /opt/hockeyplanner/backend-src
            git pull origin master
            APP_VERSION="$(cat /opt/hockeyplanner/frontend-src/VERSION | tr -d '[:space:]')"
            APP_COMMIT="$(git rev-parse --short HEAD)"
            APP_BUILD_TIME="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"

            cd /opt/hockeyplanner
            APP_VERSION="$APP_VERSION" APP_COMMIT="$APP_COMMIT" APP_BUILD_TIME="$APP_BUILD_TIME" docker compose build backend

            docker ps -a --format '{{.Names}}' | grep 'hockeyplanner-backend' | xargs -r docker rm -f

            APP_VERSION="$APP_VERSION" APP_COMMIT="$APP_COMMIT" APP_BUILD_TIME="$APP_BUILD_TIME" docker compose up -d backend
            docker compose ps
